# TMUX 세팅 백업/복구 가이드 (2026-02-13)

## 1) 백업 위치

- 저장소: `git@github.com:TwoTwo-me/Reports.git`
- 경로: `tmux-backup/2026-02-13/`
  - `home/tmux.conf`
  - `bin/` (tmux/codex/zai 관련 스크립트 15개)
  - `systemd/` (tmux 부팅/키 주입/codex poll unit 4개)

## 2) 포함 파일 범위

- tmux 메인 설정
  - `home/tmux.conf`
- 상태바/클릭/쿼터/유틸 스크립트
  - `bin/tmux-status-click.sh`
  - `bin/tmux-pane-list.sh`
  - `bin/tmux-git-segment.sh`
  - `bin/tmux-path-right.sh`
  - `bin/tmux-codex-quota.sh`
  - `bin/tmux-zai-quota.sh`
  - `bin/tmux-path-git.sh`
  - `bin/tmux-right-stats.sh`
  - `bin/tmux-sys-stats.sh`
  - `bin/tmux-load-opencode-key.sh`
  - `bin/tmux-zai-healthcheck.sh`
  - `bin/tmux-boot-key-sync.sh`
  - `bin/tmux-load-zai-key.sh`
  - `bin/codex-quota-poll.sh` (레거시 보관)
  - `bin/codex-quota-set` (레거시 보관)
- systemd unit
  - `systemd/ttyd-tmux.service`
  - `systemd/tmux-zai-key-bootstrap.service`
  - `systemd/codex-quota-poll.service`
  - `systemd/codex-quota-poll.timer`

## 3) 복구 절차

아래는 루트 사용자 기준 복구 예시다.

```bash
# 1) 설정 복원
cp tmux-backup/2026-02-13/home/tmux.conf /root/.tmux.conf

# 2) 스크립트 복원
cp tmux-backup/2026-02-13/bin/* /root/.local/bin/
chmod +x /root/.local/bin/tmux-*.sh /root/.local/bin/codex-quota-poll.sh /root/.local/bin/codex-quota-set

# 3) systemd unit 복원
cp tmux-backup/2026-02-13/systemd/*.service /etc/systemd/system/
cp tmux-backup/2026-02-13/systemd/*.timer /etc/systemd/system/
systemctl daemon-reload

# 4) 필요 시 활성화
systemctl enable --now ttyd-tmux.service tmux-zai-key-bootstrap.service
systemctl enable --now codex-quota-poll.timer

# 5) tmux 반영
tmux source-file /root/.tmux.conf
```

## 4) 운영 확인

```bash
tmux show-options -g | grep -E '^status |^status-format\[[0-9]+\]|^mouse|^base-index|^renumber-windows'
tmux list-keys -T root | grep MouseDown1Status
tmux list-keys -T prefix | grep 'bind-key -T prefix g '
systemctl is-active ttyd-tmux.service tmux-zai-key-bootstrap.service
```

## 5) 민감정보 정책

- 이 백업에는 비밀키/토큰 실데이터를 넣지 않았다.
- 다음 파일은 의도적으로 제외했다.
  - `/root/.local/share/opencode/auth.json`
  - `pass` 저장소 데이터
- 복구 후 인증이 필요하면 아래를 사용한다.
  - `opencode auth login openai`
  - `/root/.local/bin/tmux-load-opencode-key.sh`
